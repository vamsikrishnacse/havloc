import 'package:flutter/material.dart';

class StudentFeedbacks extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: const Center(
        child: Text('Feedbacks'),
      ),
    );}
}
