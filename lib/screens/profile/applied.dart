import 'package:flutter/material.dart';

class StudentApplied extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: const Center(
        child: Text('Applied'),
      ),
    );}
}
